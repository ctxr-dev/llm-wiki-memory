# Skill: write a release migration (scripts/migrations)

Canonical copy: `.claude/skills/write-migration/SKILL.md`. Applies to the
llm-wiki-memory engine repo ONLY — never a consuming project.

A breaking change ships an idempotent, state-detecting migration, not a runbook.
Criteria and checklist: `.agents/rules/release-migrations-authoring.md`.
